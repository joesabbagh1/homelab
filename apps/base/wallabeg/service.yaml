apiVersion: v1
kind: Service
metadata:
  name: wallabeg
  namespace: wallabeg
  labels:
    app: wallabeg
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      NodePort: 30020
  selector:
    app.kubernetes.io/name: wallabeg
